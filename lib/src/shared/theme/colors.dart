import 'package:flutter/material.dart';

class AppColors {
  // Corporate Palette
  static const primary = Color(0xFF3F51B5); // Indigo
  static const secondary = Color(0xFF03A9F4); // Light Blue
  static const accent = Color(0xFF03A9F4); // Same as secondary
  static const background = Color(0xFFF5F5F5); // Light Gray
  static const surface = Colors.white;
  static const error = Color(0xFFD32F2F); // Material Red

  // Text Colors
  static const textPrimary = Color(0xFF212121); // Almost Black
  static const textSecondary = Color(0xFF616161); // Medium Gray
  static const textInverse = Colors.white;

  // Dark Mode Colors (kept for compatibility)
  static const darkBackground = Color(0xFF121212);
  static const darkSurface = Color(0xFF1E1E1E);
  static const darkSurfaceVariant = Color(0xFF2C2C2C);
}
